# Dash 深色主题切换器

## 快速开始

### 本地运行
```bash
workon crypt
pip install -r requirements.txt
python app.py


# 开发模式
docker-compose up dev

# 生产模式
docker-compose up -d prod


git add .
git commit -m "message"
git push